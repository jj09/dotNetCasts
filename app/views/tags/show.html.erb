<p id="notice"><%= notice %></p>

<%= @tag.name %>
<%= link_to 'Edit', edit_tag_path(@tag) %> |
<%= link_to 'Back', tags_path %>

<ul>
<% @tag.vodcasts.each do |vodcast| %>
	<li><%= link_to vodcast.title, vodcast %></li>
<% end %>
</ul>