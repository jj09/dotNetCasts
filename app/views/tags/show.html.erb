<h2><%= @tag.name %></h2>

<% if admin? %>
	<p>
		<%= link_to 'Edit', edit_tag_path(@tag) %>
	</p>
<% end %>

<ul class="unstyled">
<% @tag.vodcasts.each do |vodcast| %>
	<li><%= link_to vodcast.title, vodcast %></li>
<% end %>
</ul>